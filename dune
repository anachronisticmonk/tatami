; web/ holds the page, and is not a build directory.
;
; schema/ used to be listed here too, back when it held hand-written stand-ins
; that were only ever read as data. It is compiled now: the generated loaders
; call `Repo.make` and the getters, so the modules have to exist. Its .mli are
; still read as data as well, by Tatami.Schema.load_dir, which is unaffected --
; see schema/dune.
(data_only_dirs web)
