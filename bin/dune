; One stanza per executable. A single (executables) stanza links every listed
; module into every binary, so each one would run the others' top-level effects.

(executable
 (name gen_corpus)
 (modules gen_corpus)
 (libraries unix))
